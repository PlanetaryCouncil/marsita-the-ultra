# Copy to .env locally. NEVER commit .env or real secrets.

# SQLite database path (use a persistent-volume path in production).
PC_DB_PATH=data/planetary_council.db

# Telegram bot — leave empty until the real bot is wired (growth step 2).
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
